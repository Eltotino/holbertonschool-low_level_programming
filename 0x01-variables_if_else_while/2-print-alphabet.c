#include <stdio.h>
/**
 * main - Entry Point
 *
 * i made it
 *Return: Always 0 (Success)
 */
int main(void)
{
	char bb;
	 bb = 'a';

	while (bb <= 'z')
	{
	putchar(bb);
	bb++;
			}
	putchar('\n');
	return (0);
}
