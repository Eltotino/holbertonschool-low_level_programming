#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Entry Point
 *
 * i made it
 *Return: Always 0 (Success)
 */
int main(void)
{
	char bb;

	for (bb = 'a'; bb <= 'z'; ++bb)
	putchar("%c ", bb);
	return (0);
}
